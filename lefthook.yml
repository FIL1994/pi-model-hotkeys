pre-commit:
  parallel: true
  commands:
    lint:
      run: bun run lint
    format:
      run: bun run format:check

commit-msg:
  commands:
    conventional:
      run: bunx --no-install commitlint --edit {1}
